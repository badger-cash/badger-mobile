describe("tokens::reducer", () => {
  it.todo("initializes to initialState");
  it.todo("updates token metadata begin");
  it.todo("updates token metadata fail");
  it.todo("updates token metadata success");
  it.todo("updates token metadata success multiple at a time");
});
