// @flow
import React from "react";
import styled from "styled-components";
import { SafeAreaView } from "react-native";

import { T } from "../atoms";

type Props = {};

const SendConfirmScreen = ({  }: Props) => {
  return (
    <SafeAreaView>
      <T>Send Confirm Screen</T>
    </SafeAreaView>
  );
};

export default SendConfirmScreen;
