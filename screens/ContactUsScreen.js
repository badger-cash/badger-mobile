// @flow

import React from "react";
import { SafeAreaView } from "react-native";

import { H2, T, Spacer } from "../atoms";

type Props = {};
const ContactUsScreen = (props: Props) => {
  return (
    <SafeAreaView>
      <T>Contact Us</T>
    </SafeAreaView>
  );
};

export default ContactUsScreen;
